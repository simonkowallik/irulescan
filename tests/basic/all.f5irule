when RULE_INIT {
    set zero [expr 0 + 0]

    # expr $zero / [set div_by 0]

    expr "1 + 1"

    if "true == true" {
    } elseif "false == true" {
    }

    switch [set two 2] \
        2 {} \
        1 {}

    eval "expr 3 * 3"

    if "[catch "expr {1/$zero}"]" "
        [puts "Caught error"]
    "

    class match -name {name} equals datagroup

    if "[class match -name {ifname} equals ifdatagroup]" "set x 1"

    regexp -all {[0-7]} $string

    log local0. "deprecated? [client_addr]"

    if { 10 }
    {
        expr 10
    }
    else
    {
        expr 11
    }

    set heiß [expr "(✨ + 🪵) == 🔥"]
}